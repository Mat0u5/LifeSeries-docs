Fixed a crash issue when launching a server with Fabric API in 1.20 - 1.20.6

----------

Added an event for when a *justified* kill happens.
Rewrote packet handling to hopefully improve stability.

----------

Added schedule safe player references.
Update packets now get periodically sent.

----------

Subins now respect tamed animals.

----------

Flashback now properly gets detected on the client in newer versions.
/subin list can now be run by anyone.

----------

Added the 'lives_randomize_pseudorandom' global config - If there are enough players, it forces at least one occurrence of every possible life amount. Default value is true (This has always been like this btw, only made it configurable).
The colorblind support setting no longer shows team names if they are empty.

----------

Talking while being muted via the dead players mute / watcher mute now allows you to talk to other muted players and admins.

----------

Added the 'muted_chat' global config. Default value is true.
Added the 'muted_chat_admin' broadcast global config. Default value is true.

----------

You can now scroll by dragging the scroll bar in the config.

----------

Added the 'teams_system_disabled' global config. Default value is false.

----------

Fixed a launch crash issue on NeoForge 1.21.7/8

----------

Fixed an astral projection crash issue in 26.2+
You can no longer animal disguise as the new cushion entity.
Straw beds now work properly in Nice Life.

----------

Fixed an issue where Santa Bot would sometimes appear as normal Trivia Bot.
Improved current season checking.

----------

Fixed a crash issue when entering the nether in Nice Life 26.1+

----------

The -x time screen no longer shows up when final dying in Limited Life.