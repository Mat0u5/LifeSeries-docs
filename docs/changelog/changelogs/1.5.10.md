Added Modifiable Sounds, similar to Modifiable Text.

----------

Added the ability to add warnings to the config entries.
Modifiable Sounds are now playable through a button in the config itself.
Modifiable Sounds  now show an error in case of an invalid identifier and a warn in case of an unknown sound.

----------

All server-side sounds in the mod are now modifiable.
Setting modifiable sounds to be empty now plays nothing, as intended.
Deleted the 'final_death_sound' global config.

----------

Added the '/task guess ⟨player⟩ ⟨string⟩' command.
Added 'guess_tasks' Secret Life config. Default value is false.
Added 'guess_tasks_public' Secret Life config. Default value is false.
Added 'guess_tasks_life_guesser_min' Secret Life config. Default value is 2.
Added 'guess_tasks_life_guesser_max' Secret Life config. Default value is 2.
Added 'guess_tasks_life_guessed_min' Secret Life config. Default value is 3.

----------

Fixed final death messages sometimes having incorrect name colors.

----------

Added the 'watchers_see_boogey_and_society' global config, which controls whether Watchers have permissions for the '/boogeyman list' and '/society members list' commands. Default value is false.

----------

Added the 'disable_snow' Nice Life config. Default value is false.
Added the 'disable_cold_world_generation' Nice Life config. Default value is false.

----------

Added the 'disable_minimal_armor_pack' global config. Default value is false.
Modified the client side 'minimal_armor' config to now accept TRUE, FALSE or MATCH_SERVER, with MATCH_SERVER being the default.

----------

Added the moss block and spore blossom recipes for Last Life.
Added the magma cream recipe for Limited Life.
The server now reloads when switching seasons to account for changing recipes over seasons.

----------

Item NBT no longer gets cleared while playing Wild Life on <= 1.20.4

----------

Added a trivia bot run out of time event.

----------

The show life diff setting now works properly in limited life.
Fixed life gain for claimed kills when the person final dies.

----------

Fixed snails not checking whether they are landing in water.
Added the 'only_lose_hunger_in_session' global config. Default value is false.
Added the 'only_advance_time_in_session' global config. Default value is false.
Removed the Nice Life 'advance_time_not_in_session' config.