Added Modifiable Sounds, similar to Modifiable Text.

----------

Added the ability to add warnings to the config entries.
Modifiable Sounds are now playable through a button in the config itself.
Modifiable Sounds  now show an error in case of an invalid identifier and a warn in case of an unknown sound.

----------

All server-side sounds in the mod are now modifiable.
Setting modifiable sounds to be empty now plays nothing, as intended.
Deleted the 'final_death_sound' global config.

----------

Added the '/task guess ⟨player⟩ ⟨string⟩' command.
Added 'guess_tasks' Secret Life config. Default value is false.
Added 'guess_tasks_public' Secret Life config. Default value is false.
Added 'guess_tasks_life_guesser_min' Secret Life config. Default value is 2.
Added 'guess_tasks_life_guesser_max' Secret Life config. Default value is 2.
Added 'guess_tasks_life_guessed_min' Secret Life config. Default value is 3.

----------

