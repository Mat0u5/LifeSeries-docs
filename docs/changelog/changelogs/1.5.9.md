Added the 'only_lose_durability_in_session' global config. Default value is false.
Priority scheduled tasks now get properly ticked when the game is frozen.
Fixed the session freeze outside session not unfreezing the session when toggled off.

----------

Added the '/session actions' command, which shows you the current queued session actions and allows you to remove them.
Reworked all clickable texts to always have a hover text event.

----------

Added the '/mobswap swap' command, which manually triggers a mob swap if the wildcard is active.
Renamed 'only_take_lives_in_session' config to 'only_change_lives_in_session'.
Having 'only_change_lives_in_session' turned on now prevents the gaining of lives through killing.

----------

Added the 'limited_life_actionbar_time' client config. Default value is false.
Season specific client configs are now only available in that specific season.

----------

You can now set config values through the command to an empty string.
Added the 'wildcard_superpowers_mimicry_disable_other_power' Wild Life config. Default value is false.

----------

The Necromancy superpower no longer sets the power owner as a zombie.

----------

When 'teams_system_disabled' is on, some life-based colors will now be changed to team based colors.
Added the 'wildcard_snails_kill_any_player' Wild Life config, which makes the snails kill any player they touch, not just their bound player. Default value is false.

----------

Config item lists now support item tags.
All seasons that banned all helmets now use the '#minecraft:head_armor' tag instead.

----------

Added the 'instantly_activate_wildcards' Wild Life config. Default value is false.
Added '${green_player}', '${yellow_player}' and '${red_player}' to the available Secret Life task formats, replaced with a random player of the given color, if available.

----------

Added the 'wildcard_trivia_effects' Wild Life config. Default value is '[speed, haste, strength, jump_boost, resistance, regeneration, fire_resistance, water_breathing, night_vision, health_boost, absorption]'
The final ~30s countdown of your own trivia bot are now heard from any distance.

----------
----------

Added the 'lives_randomize_custom_average' global config. Default value is false.
Added the 'lives_randomize_average' global config. Default value is 4.0. Limited life has a time counterpart.

----------

Fixed missing server-side message translations.

----------

Added '${name!=player}' to the available Secret Life task formats, which prevent the player to get a specific task.
Added 'soulbound_secret_society' Double Life config. Default value is false.

----------

The 'Win Secret Life.' task is now configurable via modifiable texts.
Added the 'pause_session_time_until_roll' Limited Life config. Default value is false.

----------

Added the '/task append ⟨player/s⟩ ⟨string⟩' command.
Added the 'unbreakable_buttons' Secret Life config. Default value is true.

----------

Added the Nice Life reconstructed voting sound, huge thanks to CatKing for stitching it together!

----------

Limited life now supports adding new teams for life count!
You can now use hour formatting in the limited life teams manager.

----------
----------

Added a soulmate manager to the config for Double Life.

----------

Added '${boogeypocalypse}' to the available Secret Life task formats, which triggers the infection.
