Players can now once again take outside world border damage if the 'worldborder_outside_teleport' config is set to false.
The advancedDeath flag in the '/lives remove' command now works as intended in Limited Life.

----------

Team colors now get instantly sent out to clients after modifying the team manager.
The Limited Life timer color will now match your life color.
Commands in Limited Life will now show the correct life color.

----------

The session transcript now only logs commands that admins can se feedback from.
The title that shows when a player ran out of lives/time now shows 'You ...' for the player that died.

----------

Added a Nice/Wild life config for specifying the trivia GUI type (the 'modern' one or the original). Allowed values are NICE_LIFE and WILD_LIFE.
Added the 'night_length' Nice Life config, allowed values are 'FIRST_LONG', 'ALL_LONG' and 'ALL_SHORT'.

----------

Old snail skins are now converted server-side to the new format, replacing the old legacy formats.
The old replaced files are kept and moved into an unused '_legacy_skins' folder.
Life skins now supports old 64x32 skins, again those are automatically converted to the new format with the old ones being stored in a separate folder.
The provided blockbench model for snail skins now uses the new snail skins format instead of the old.
Life skins now allows life numbers even if there is no team for that amount of lives.

----------

The session end event no longer triggers when the previous state was FINISHED or NOT_STARTED.
The setSeries command now accepts optional arguments from the following list: `-noGui`, `-noChatFeedback`, `-keepLives`, `-keepSession`.

----------
